# Plan: 2
# Generated by: discovery:2.d
# Timestamp: 2025-12-21T21:50:00Z

story_id: "2"
title: "ClaudeCodeNotFoundError"
created: "2025-12-21T21:50:00Z"
created_by: "discovery:2.d"

# Analysis Summary
analysis:
  summary: "Enhance existing CLINotFoundError class with comprehensive installation instructions covering multiple platforms and installation methods (npm, Homebrew, pip extras)"
  complexity: "low"
  estimated_files: 1
  estimated_tokens: 2000
  risk_factors:
    - "Breaking changes to existing error message format may affect dependent code"
    - "Platform detection needed for macOS-specific Homebrew instructions"

# Files to Create
files_to_create: []

# Files to Modify
files_to_modify:
  - path: "src/claude_agent_sdk/_errors.py"
    changes:
      - "Rename CLINotFoundError to ClaudeCodeNotFoundError for clarity (keep CLINotFoundError as alias for backwards compatibility)"
      - "Add default comprehensive error message with installation instructions"
      - "Include npm install command (global installation)"
      - "Include Homebrew installation for macOS"
      - "Include pip install with auto-oauth extra option"
      - "Add link to official Anthropic documentation"
      - "Add platform detection logic for conditional Homebrew instructions"
    lines_affected: 20

  - path: "src/claude_agent_sdk/_internal/transport/subprocess_cli.py"
    changes:
      - "Update import to use ClaudeCodeNotFoundError (maintain CLINotFoundError reference)"
      - "Remove inline error message from _find_cli() method (use exception's default message)"
      - "Add platform detection context when raising exception"
    lines_affected: 10

# Integration Points
integration:
  - file: "src/claude_agent_sdk/_errors.py"
    type: "export"
    description: "Export ClaudeCodeNotFoundError from _errors module (maintain CLINotFoundError export for backwards compatibility)"

  - file: "src/claude_agent_sdk/__init__.py"
    type: "export"
    description: "Add ClaudeCodeNotFoundError to public API exports (check if CLINotFoundError is currently exported)"

  - file: "src/claude_agent_sdk/_internal/transport/subprocess_cli.py"
    type: "import"
    description: "Update error raising logic to use enhanced ClaudeCodeNotFoundError"

# Patterns to Follow
patterns:
  - source: "src/claude_agent_sdk/_errors.py"
    pattern: "ProcessError class shows good example of building multi-line error messages in __init__ with conditional sections"

  - source: "src/claude_agent_sdk/_errors.py"
    pattern: "Exception classes inherit from base ClaudeSDKError for proper exception hierarchy"

  - source: "src/claude_agent_sdk/_internal/oauth_credentials.py"
    pattern: "CredentialsError/CredentialsNotFoundError show pattern for creating specific error subclasses"

# Test Requirements
test_requirements:
  unit:
    - "Test ClaudeCodeNotFoundError instantiation with default message"
    - "Test error message includes npm install command"
    - "Test error message includes Homebrew install on macOS"
    - "Test error message includes pip auto-oauth extra option"
    - "Test error message includes Anthropic documentation link"
    - "Test backwards compatibility alias CLINotFoundError still works"
    - "Test cli_path parameter adds path to error message"
    - "Test platform-specific message variations (macOS vs other platforms)"
  integration:
    - "Test SubprocessCLITransport._find_cli() raises ClaudeCodeNotFoundError when CLI not found"
    - "Test error message displayed to user contains all required installation methods"
    - "Verify existing code using CLINotFoundError continues to work"
  security:
    - "Verify error messages do not expose sensitive system paths unnecessarily"
    - "Ensure documentation URLs are official Anthropic domains only"

# Acceptance Criteria Mapping
acceptance_criteria:
  - id: "AC-2.1"
    text: "(P0) ClaudeCodeNotFoundError exception class defined"
    implementation: "files_to_modify[0] - Rename and enhance CLINotFoundError"
    tests: "test_requirements.unit[0,5]"

  - id: "AC-2.2"
    text: "(P0) Error message includes npm install command"
    implementation: "files_to_modify[0] - Add npm install to default message"
    tests: "test_requirements.unit[1]"

  - id: "AC-2.3"
    text: "(P1) Error message includes Homebrew install for macOS"
    implementation: "files_to_modify[0] - Add platform detection and Homebrew instructions"
    tests: "test_requirements.unit[2,7]"

  - id: "AC-2.4"
    text: "(P1) Error message includes auto-oauth pip extra option"
    implementation: "files_to_modify[0] - Add pip install instructions with [auto-oauth] extra"
    tests: "test_requirements.unit[3]"

  - id: "AC-2.5"
    text: "(P2) Error message includes link to Anthropic docs"
    implementation: "files_to_modify[0] - Add documentation URL to error message"
    tests: "test_requirements.unit[4], test_requirements.security[1]"

# Implementation Notes
implementation_notes:
  backwards_compatibility:
    - "Keep CLINotFoundError as type alias to ClaudeCodeNotFoundError"
    - "Maintain same constructor signature (message, cli_path)"
    - "Ensure existing exception handling code continues to work"

  platform_detection:
    - "Use platform.system() to detect macOS for Homebrew instructions"
    - "Keep Homebrew section optional (only show on macOS or if user explicitly needs it)"

  message_structure:
    - "Clear heading: 'Claude Code CLI not found'"
    - "Primary method: npm global install (works on all platforms)"
    - "Alternative method 1: Homebrew (macOS only, conditional)"
    - "Alternative method 2: pip install SDK with [auto-oauth] extra"
    - "Additional help: Link to documentation"
    - "Developer option: ClaudeAgentOptions(cli_path=...) for custom paths"

  documentation_url:
    - "Use official Anthropic documentation URL"
    - "Suggest: https://docs.anthropic.com/en/docs/claude-code/installation"
    - "Verify URL exists and is stable before hardcoding"
