# Plan: 6
# Generated by: discovery:6.d
# Timestamp: 2025-12-21T22:35:00Z

story_id: "6"
title: "Documentation Update"
created: "2025-12-21T22:35:00Z"
created_by: "discovery:6.d"

# Analysis Summary
analysis:
  summary: "Update SDK documentation with installation requirements, OAuth prerequisites, and troubleshooting guidance for ClaudeCodeNotFoundError"
  complexity: "low"
  estimated_files: 2
  estimated_tokens: 4000
  risk_factors:
    - "None - documentation-only changes"
    - "Need to ensure consistency across README.md and examples/README.md"

# Current State Assessment
current_state:
  readme_main:
    status: "Partially complete"
    location: "README.md"
    what_exists:
      prerequisites_section:
        - "Lines 11-14: Lists Python 3.10+, Node.js, Claude Code 2.0.0+"
        - "Installation command: npm install -g @anthropic-ai/claude-code"
        - "✅ Already documents Claude Code CLI as prerequisite"

      oauth_section:
        - "Lines 29-61: OAuth Authentication (Subscription) section"
        - "Lines 36-48: First-time setup with automatic browser login"
        - "Lines 50-60: Manual login command (claude /login)"
        - "✅ Already explains OAuth requires Claude Code CLI"

      troubleshooting_section:
        - "Lines 143-163: Authentication troubleshooting"
        - "Lines 145-147: OAuth credentials not found"
        - "Lines 149-152: OAuth credentials expired"
        - "Lines 154-157: Browser login not available"
        - "Lines 159-162: Fallback behavior"
        - "⚠️ Missing ClaudeCodeNotFoundError troubleshooting"
        - "⚠️ Missing CLI detection issues"

      error_handling_section:
        - "Lines 409-431: Error Handling section"
        - "Lines 411-418: Lists all error types"
        - "Line 414: CLINotFoundError (old name)"
        - "❌ Missing ClaudeCodeNotFoundError"
        - "❌ No installation instructions in error handling docs"
        - "❌ No reference to auto-oauth extra"

      installation_section:
        - "Lines 5-14: Installation section"
        - "Line 8: Basic pip install claude-agent-sdk"
        - "❌ Missing auto-oauth extra option"
        - "❌ Missing Homebrew option (macOS)"

    gaps:
      - "ClaudeCodeNotFoundError not documented in Error Handling section"
      - "Auto-oauth extra installation option not mentioned"
      - "Homebrew installation option not mentioned (macOS)"
      - "CLI detection troubleshooting not in main troubleshooting section"
      - "Error handling example still uses old CLINotFoundError name"

  examples_readme:
    status: "Complete for OAuth basics"
    location: "examples/README.md"
    what_exists:
      prerequisites_section:
        - "Lines 6-16: Prerequisites section"
        - "Lines 7-10: npm install -g @anthropic-ai/claude-code"
        - "Lines 12-16: claude /login for OAuth setup"
        - "✅ Already documents Claude Code CLI requirement"

      troubleshooting_section:
        - "Lines 90-109: Troubleshooting section"
        - "Lines 92-96: OAuth credentials not found"
        - "Lines 98-102: Claude Code not installed"
        - "Lines 104-108: Module not found"
        - "✅ Covers basic installation issues"
        - "⚠️ Could add more detail on CLI detection issues"

    gaps:
      - "Could add reference to auto-oauth extra as alternative installation method"
      - "Could add Homebrew installation option (macOS)"

  error_class_implementation:
    status: "Complete"
    location: "src/claude_agent_sdk/_errors.py"
    what_exists:
      - "Lines 14-58: ClaudeCodeNotFoundError class"
      - "Lines 21-48: Comprehensive installation instructions"
      - "Lines 30-31: npm installation (recommended)"
      - "Lines 34-36: Homebrew installation (macOS only)"
      - "Lines 39-44: pip with auto-oauth extra"
      - "Lines 47-48: Documentation link"
      - "Lines 51-52: Custom cli_path option"
      - "Line 62: CLINotFoundError alias for backward compatibility"
      - "✅ Error class has all installation instructions built-in"

    api_documentation:
      - "❌ No API reference documentation exists"
      - "❌ Error class not documented in README API docs"

# Gap Analysis
gaps:
  priority_0_critical:
    - id: "GAP-6.1"
      description: "README Installation section missing auto-oauth extra option"
      location: "README.md lines 5-14"
      impact: "Users don't know about auto-install feature"
      recommendation: "Add pip install 'claude-agent-sdk[auto-oauth]' option"

    - id: "GAP-6.2"
      description: "README Error Handling section missing ClaudeCodeNotFoundError"
      location: "README.md lines 409-431"
      impact: "Users don't know about new error class or installation options"
      recommendation: "Add ClaudeCodeNotFoundError to error handling examples"

  priority_1_important:
    - id: "GAP-6.3"
      description: "README Installation section missing Homebrew option"
      location: "README.md lines 5-14"
      impact: "macOS users don't know about Homebrew installation"
      recommendation: "Add Homebrew installation option for macOS"

    - id: "GAP-6.4"
      description: "README Troubleshooting section missing CLI detection issues"
      location: "README.md lines 143-163"
      impact: "Users can't troubleshoot CLI not found errors"
      recommendation: "Add CLI detection troubleshooting subsection"

    - id: "GAP-6.5"
      description: "examples/README.md missing auto-oauth extra reference"
      location: "examples/README.md lines 90-109"
      impact: "Example users don't know about auto-install option"
      recommendation: "Add auto-oauth extra to troubleshooting"

  priority_2_nice_to_have:
    - id: "GAP-6.6"
      description: "No dedicated API reference documentation"
      location: "N/A - new file needed"
      impact: "Developers need to read source code for error class details"
      recommendation: "Consider adding API.md or expanding types section"

# Content Outline
content_outline:
  readme_changes:
    installation_section:
      location: "Lines 5-14"
      changes:
        - type: "add"
          priority: "P0"
          content: |
            Add auto-oauth extra installation option:
            ```bash
            # Option 1: Basic installation (requires manual Claude Code CLI install)
            pip install claude-agent-sdk

            # Option 2: Auto-install Claude Code CLI (recommended)
            pip install 'claude-agent-sdk[auto-oauth]'
            ```

        - type: "add"
          priority: "P1"
          content: |
            Add Homebrew installation option for macOS in Prerequisites:
            ```bash
            # macOS users can also use Homebrew
            brew install claude-code
            ```

    error_handling_section:
      location: "Lines 409-431"
      changes:
        - type: "replace"
          priority: "P0"
          old_content: "CLINotFoundError"
          new_content: "ClaudeCodeNotFoundError"
          details: "Update example to use new error class name"

        - type: "add"
          priority: "P0"
          content: |
            Add comprehensive ClaudeCodeNotFoundError example:
            ```python
            try:
                async for message in query(prompt="Hello"):
                    pass
            except ClaudeCodeNotFoundError as e:
                # Error message includes installation instructions:
                # 1. npm install -g @anthropic-ai/claude-code (recommended)
                # 2. brew install claude-code (macOS)
                # 3. pip install 'claude-agent-sdk[auto-oauth]'
                print(f"Claude Code CLI not found: {e}")
            ```

    troubleshooting_section:
      location: "Lines 143-163"
      changes:
        - type: "add"
          priority: "P1"
          content: |
            Add CLI Detection subsection:

            **"Claude Code CLI not found"**
            - Install via npm: `npm install -g @anthropic-ai/claude-code`
            - Install via Homebrew (macOS): `brew install claude-code`
            - Auto-install with pip: `pip install 'claude-agent-sdk[auto-oauth]'`
            - Custom path: `ClaudeAgentOptions(cli_path='/path/to/claude')`
            - Verify installation: Run `claude --version` in terminal

            **"CLI detection issues"**
            - Ensure `claude` is in your PATH
            - Check `~/.claude/bin/claude` exists (local install)
            - Windows: Check for `claude.exe` or `claude.cmd`
            - macOS Homebrew: Check `/opt/homebrew/bin/claude`
            - See error message for detailed installation instructions

  examples_readme_changes:
    troubleshooting_section:
      location: "Lines 90-109"
      changes:
        - type: "add"
          priority: "P1"
          content: |
            Add auto-oauth extra option to "Claude Code not installed" section:
            ```bash
            # Option 1: Install via npm (recommended)
            npm install -g @anthropic-ai/claude-code

            # Option 2: Install via Homebrew (macOS only)
            brew install claude-code

            # Option 3: Auto-install with SDK
            pip install 'claude-agent-sdk[auto-oauth]'
            ```

# File Changes Summary
file_changes:
  - path: "README.md"
    sections_modified:
      - name: "Installation"
        lines: "5-14"
        changes:
          - "Add auto-oauth extra installation option"
          - "Add Homebrew option to Prerequisites"

      - name: "Error Handling"
        lines: "409-431"
        changes:
          - "Replace CLINotFoundError with ClaudeCodeNotFoundError in example"
          - "Add comprehensive error handling example with installation options"

      - name: "Troubleshooting"
        lines: "143-163"
        changes:
          - "Add CLI Detection subsection"
          - "Document installation verification steps"
          - "Document platform-specific CLI paths"

    estimated_additions: "+40 lines"
    estimated_deletions: "-3 lines"

  - path: "examples/README.md"
    sections_modified:
      - name: "Troubleshooting"
        lines: "90-109"
        changes:
          - "Expand Claude Code installation section with all options"
          - "Add auto-oauth extra reference"

    estimated_additions: "+8 lines"
    estimated_deletions: "-2 lines"

# Implementation Strategy
implementation_strategy:
  phase_1_readme_main:
    order: 1
    description: "Update main README.md with all documentation gaps"
    tasks:
      - "Update Installation section with auto-oauth extra"
      - "Add Homebrew to Prerequisites"
      - "Update Error Handling section with ClaudeCodeNotFoundError"
      - "Add CLI Detection to Troubleshooting section"

  phase_2_examples_readme:
    order: 2
    description: "Update examples/README.md with installation options"
    tasks:
      - "Expand troubleshooting with all installation methods"
      - "Add auto-oauth extra reference"

  phase_3_validation:
    order: 3
    description: "Validate documentation consistency"
    tasks:
      - "Verify installation instructions consistent across both files"
      - "Check error names are up-to-date"
      - "Verify links work"
      - "Review formatting and clarity"

# Patterns to Follow
patterns:
  existing_style:
    - source: "README.md"
      pattern: "Use bash code blocks for commands, python blocks for code examples"

    - source: "README.md"
      pattern: "Use **bold** for error messages in troubleshooting"

    - source: "examples/README.md"
      pattern: "Show platform-specific commands (Windows Git Bash vs Command Prompt vs macOS/Linux)"

  error_class_reference:
    - source: "src/claude_agent_sdk/_errors.py"
      pattern: "Error class already contains comprehensive installation instructions"

    - source: "src/claude_agent_sdk/_errors.py"
      pattern: "Three installation methods: npm (recommended), Homebrew (macOS), auto-oauth extra"

# Acceptance Criteria Mapping
acceptance_criteria:
  - id: "AC-6.1"
    text: "(P0) README documents Claude Code CLI as prerequisite for OAuth"
    current_status: "✅ Already complete (lines 11-14)"
    implementation: "No changes needed"
    validation: "Verify Prerequisites section mentions Claude Code CLI"

  - id: "AC-6.2"
    text: "(P1) README shows installation options (npm, Homebrew, auto-oauth extra)"
    current_status: "⚠️ Partially complete - npm exists, missing Homebrew and auto-oauth"
    implementation: "file_changes[0].sections_modified[0] - Update Installation section"
    validation: "All three installation methods documented"

  - id: "AC-6.3"
    text: "(P1) API docs updated for ClaudeCodeNotFoundError"
    current_status: "❌ Missing - Error Handling uses old CLINotFoundError"
    implementation: "file_changes[0].sections_modified[1] - Update Error Handling section"
    validation: "Error handling example uses ClaudeCodeNotFoundError with installation guidance"

  - id: "AC-6.4"
    text: "(P2) Troubleshooting section for common CLI detection issues"
    current_status: "❌ Missing - No CLI detection troubleshooting"
    implementation: "file_changes[0].sections_modified[2] - Add CLI Detection subsection"
    validation: "Troubleshooting covers CLI not found, PATH issues, platform-specific paths"

# Success Criteria
success_criteria:
  - "README.md Installation section shows all three installation methods"
  - "README.md Error Handling uses ClaudeCodeNotFoundError"
  - "README.md Troubleshooting has CLI Detection subsection"
  - "examples/README.md Troubleshooting references auto-oauth extra"
  - "Documentation consistent across both README files"
  - "All installation instructions accurate and up-to-date"
  - "Platform-specific guidance clear (macOS Homebrew, Windows extensions)"

# Additional Notes
notes:
  strengths:
    - "Most OAuth documentation already exists and is comprehensive"
    - "ClaudeCodeNotFoundError implementation has excellent error messages"
    - "Prerequisites already mention Claude Code CLI requirement"

  opportunities:
    - "Update error handling to showcase new ClaudeCodeNotFoundError"
    - "Add auto-oauth extra as recommended installation method"
    - "Expand troubleshooting for CLI detection issues"

  dependencies:
    - "Story 2: ClaudeCodeNotFoundError (provides error class to document)"
    - "Story 4: Optional Auto-OAuth Extra (provides installation method to document)"

  future_considerations:
    - "Consider dedicated API.md for detailed error class documentation"
    - "Consider adding inline code comments to examples for installation troubleshooting"
    - "Consider adding FAQ section for common OAuth + CLI issues"
